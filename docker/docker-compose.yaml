name: hsm-chatbot

networks:
  hsm-chatbot-network:
    name: hsm-chatbot-network
    driver: bridge

volumes:
  pgdata:
    name: hsm-chatbot-postgres-data
  redisdata:
    name: hsm-chatbot-redis-data
  miniodata:
    name: hsm-chatbot-minio-data

services:
  hsm-chatbot-base:
    image: hsm-chatbot-base
    container_name: hsm-chatbot-base
    build:
      context: ../
      dockerfile: ./docker/dockerFile.base

  hsm-chatbot-main:
    image: hsm-chatbot-main
    container_name: hsm-chatbot-main
    build:
      context: ../
      args:
        - APP_TYPE=main
      dockerfile: ./docker/dockerFile.dev
    ports:
      - 32000:3000
    networks:
      - hsm-chatbot-network
    volumes:
      - ../:/app
      - /app/node_modules
    env_file:
      - ../.env
    environment:
      SECRET_PATH: /chatbot
    restart: unless-stopped
    depends_on:
      - hsm-chatbot-base
    #  - hsm-chatbot-db-postgres
    #  - hsm-chatbot-db-redis
    #  - hsm-chatbot-db-minio

  hsm-chatbot-db-postgres:
    image: postgres:17-bookworm
    container_name: hsm-chatbot-db-postgres
    ports:
      - 5432:5432
    networks:
      - hsm-chatbot-network
    volumes:
      - ./postgres/init-schemas.sql:/docker-entrypoint-initdb.d/postgres.schemas.sql:ro
      - pgdata:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: hsm
    restart: unless-stopped

  hsm-chatbot-db-redis:
    image: redis:8-bookworm
    container_name: hsm-chatbot-db-redis
    ports:
      - 6379:6379
    networks:
      - hsm-chatbot-network
    volumes:
      - ./redis/redis.conf:/usr/local/etc/redis/redis.conf:ro
      - ./redis/users.acl:/usr/local/etc/redis/users.acl:ro
      - redisdata:/data
    environment:
      REDIS_USER: redis
      REDIS_PASSWORD: redis
    command: [redis-server, /usr/local/etc/redis/redis.conf]
    restart: unless-stopped

  hsm-chatbot-db-minio:
    image: minio/minio:latest
    container_name: hsm-chatbot-db-minio
    networks:
      - hsm-chatbot-network
    environment:
      MINIO_ROOT_USER: minio_user
      MINIO_ROOT_PASSWORD: minio_password
    ports:
      - 19000:9000
      - 19001:9001
    volumes:
      - miniodata:/data
    command: server /data --console-address ":9001"
    restart: unless-stopped

  redisinsight:
    image: redis/redisinsight:latest
    container_name: redisinsight
    ports:
      - 5540:5540
    networks:
      - hsm-chatbot-network
    environment:
      RI_ACCEPT_TERMS_AND_CONDITIONS: true
      RI_REDIS_HOST: hsm-chatbot-db-redis
      RI_REDIS_PORT: 6379
      RI_REDIS_USERNAME: redis
      RI_REDIS_PASSWORD: redis
    restart: unless-stopped

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: pgadmin
    ports:
      - 5050:80
    volumes:
      - ./postgres/servers.json:/pgadmin4/servers.json:ro
    networks:
      - hsm-chatbot-network
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@example.com
      PGADMIN_DEFAULT_PASSWORD: secret
